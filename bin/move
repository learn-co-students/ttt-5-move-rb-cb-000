#!/usr/bin/env ruby

require_relative '../lib/move.rb'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
# Code your CLI Here"
puts"Welcome to Tic Tac Toe!"
puts"Where would you like to go?"

indexStr = gets.strip
index = input_to_index(indexStr)
char = "X"
move(board,(index),char)
display_board(board)
